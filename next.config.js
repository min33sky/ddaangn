/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['kvelbbhoxhirimsjhtxm.supabase.in'],
  },
};

module.exports = nextConfig;
